# Modernize ES6+
Refactor this legacy JavaScript code to use ES6+ features like arrow functions, destructuring, and template literals.

# Typescript Conversion
Convert this JavaScript code to TypeScript. Define interfaces for all objects and types for function parameters.

# React Functional Component
Convert this class-based React component into a functional component using Hooks (useState, useEffect).

# Promise to Async/Await
Refactor this `.then()` chain into a clean `async/await` block.

# Optimize Loops
Replace standard `for` loops with modern array methods like `.map()`, `.filter()`, or `.reduce()`.

# Destructure Props
In this React component, destructure the props object for better readability.

# Add JSDoc
Generate comprehensive JSDoc comments for this file, including `@param` and `@returns` tags.

# Error Boundary
Wrap this logic in a robust try-catch block and implement a graceful fallback mechanism.

# Performance Memoization
Wrap this expensive calculation or component in `useMemo` or `React.memo`.

# Fetch to Axios
Refactor this native `fetch` call to use the `axios` library with interceptors.

# Unit Test (Jest)
Write Jest unit tests for this function, including test cases for edge cases.

# Simplify Conditionals
Use optional chaining (`?.`) and nullish coalescing (`??`) to simplify this logic.

# Module Export
Convert these functions to use ESM `export` syntax instead of `module.exports`.

# Tailor Console Logs
Replace generic `console.log` with meaningful, structured logs or a logging library.

# Redux Toolkit
Convert this standard Redux boilerplate into a Redux Toolkit slice.

# CSS-in-JS
Convert these inline styles into a styled-components or Emotion definition.

# Input Validation
Implement Zod schema validation for this input data.

# Debounce Logic
Wrap this function in a debounce or throttle utility to prevent excessive execution.

# Remove Comments
Strip all comments from this code while keeping the logic intact.

# Flatten Hierarchy
Refactor deeply nested callback hell into a flat structure using Promises.
