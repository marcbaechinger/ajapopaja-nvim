# Create a HTML scaffolding for an single page application

* Create a plugin infrastructure to load javascript modules dynamically
* load the plugin 'app.js' as the first plugin that initializes the app
* Use the Tailwind.CSS framework

# Create a table with 4 rows and 3 cols
