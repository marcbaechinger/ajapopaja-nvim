# Accessibility (A11y)

Audit this HTML and add necessary ARIA attributes, alt tags, and roles for screen readers.

# Semantic Refactor

Replace generic `<div>` and `<span>` tags with semantic elements like `<main>`, `<article>`, `<section>`, and `<nav>`.

# SEO Optimization

Improve this HTML for search engines by adding meta tags, canonical links, and header hierarchy.

# Responsive Images

Refactor `<img>` tags to use `srcset` and `sizes` for responsive performance.

# Form Validation

Add HTML5 validation attributes (required, pattern, minlength) to these form inputs.

# Minify

Remove unnecessary whitespace and comments from this HTML structure.

# Layout to Grid

Suggest an HTML structure change to better support CSS Grid layouts.

# Dark Mode Support

Add classes or data-attributes necessary to support a dark mode toggle.

# Externalize Scripts

Identify inline scripts and provide a way to move them to external `.js` files.

# Link Security

Add `rel="noopener noreferrer"` to all target="_blank" links.

# Template Conversion

Convert this static HTML into a Handlebars or Jinja2 template.

# SVG Optimization

Clean up this inline SVG code by removing editor metadata and simplifying paths.

# Favicon Set

Generate the full set of HTML tags for modern favicons (Apple, Android, Web).

# Skeleton Screen

Create a skeleton loading state version of this HTML structure.

# Modal Structure

Refactor this into a standard, accessible Modal HTML structure.

# Navbar Refactor

Create a mobile-responsive navbar structure from this list of links.

# Table to Divs

Convert this data table into a responsive flexbox-based card layout.

# Head Cleanup

Remove redundant meta tags and optimize the order of the `<head>` elements.

# Web Component

Wrap this HTML snippet into a standard Web Component `<template>`.

# Data Attribute usage

Replace non-standard attributes with proper `data-*` attributes.

# Create a HTML scaffolding for an single page application

* Create a plugin infrastructure to load javascript modules dynamically
* load the plugin 'app.js' as the first plugin that initializes the app
* Use the Tailwind.CSS framework

# Create a table with 4 rows and 3 cols
