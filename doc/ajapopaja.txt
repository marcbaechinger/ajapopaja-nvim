*ajapopaja.txt* LLM-powered code transformation for Neovim

==============================================================================
AJAPOPAJA                                                          *ajapopaja*

A Neovim plugin designed to interface with local LLMs (via Ollama) for
asynchronous code transformation, review, and history management.

                                 Maintainer: Marc Baechinger
                                 License: MIT

==============================================================================
CONTENTS                                                  *ajapopaja-contents*

    1. Introduction ................................ |ajapopaja-intro|
    2. Installation ................................ |ajapopaja-install|
    3. Configuration ............................... |ajapopaja-config|
    4. Usage ....................................... |ajapopaja-usage|
    5. Commands .................................... |ajapopaja-commands|
    6. Keybindings ................................. |ajapopaja-mappings|
    7. History Management .......................... |ajapopaja-history|

==============================================================================
1. INTRODUCTION                                             *ajapopaja-intro*

|ajapopaja| provides a bridge between your Neovim buffer and Large Language
Models. It specializes in two main workflows:

1.  Code Transformation: Modify selected code based on natural language or
    predefined prompts. Iterate on LLM responses. Replace the original selection
    with the transformation result or insert at cursor position.
2.  Code Review: Provide actional feedback on code quality and logic and LLM
    prompts to resolve identified issues.

All LLM calls are handled asynchronously using a Python-based RPC backend to
ensure the Neovim UI remains responsive during generation.

==============================================================================
2. INSTALLATION                                           *ajapopaja-install*

REQUIRES ~
- Neovim >= 0.10.0 (Recommended)
- Python 3.10+
- Ollama (running locally or accessible via network)

PLUGIN SETUP ~
Using lazy.nvim add  `~/.config/nvim/lua/plugins/ajapopaja.lua`:
>lua
    return {
        "marcbaechinger/ajapopaja-nvim",
        build = ":UpdateRemotePlugins",
        config = function()
            require("ajapopaja").setup({
                -- Optional: defaults to http://localhost:11434
                ollama_host = "http://localhost:11434" 
            })
        end
    }
<
POST-INSTALLATION STEPS ~
Ajapopaja requires a dedicated Python virtual environment containing 
`pynvim` and `aiohttp`. To set this up:

1.  PROVISION ENVIRONMENT:
    Run the following command within Neovim:
    >vim
        :AjapopajaBootstrap
    <
    This creates a `.venv` folder inside the plugin directory and 
    installs all necessary Python dependencies asynchronously.

2.  RESTART NEOVIM:
    A full restart is required to initialize the new Python RPC host.

VERIFYING THE INSTALLATION ~
You can verify that everything is correctly configured by running:
>vim
    :checkhealth ajapopaja
<
The health check will validate your Python provider path, Ollama 
connectivity, and RPC function registration.

==============================================================================
3. CONFIGURATION                                           *ajapopaja-config*

The plugin is configured via the `setup()` function. Calling `setup()` is 
required to initialize commands and background model refreshing.

>lua
    require("ajapopaja").setup({
        -- The URL where your Ollama instance is reachable.
        ollama_host = "http://localhost:11434", -- default

        -- Set to false to disable the default keybindings.
        default_keymaps = true, -- default
    })
<

GLOBAL VARIABLES ~
*g:ajapopaja_ollama_host*
    The host address for the Ollama API. This variable is set by the 
    `setup()` function but can be manually overridden if needed for 
    specific RPC calls.

LUALINE INTEGRATION ~
Ajapopaja provides a `get_status()` function that indicates the current 
active model and a loading spinner (󱚣) when the LLM is processing a 
request.

Recommended `lualine.nvim` configuration:

>lua
    require("lualine").setup({
      sections = {
        lualine_x = {
          {
            function()
              -- Displays active model and loading state
              return require("ajapopaja").get_status()
            end,
            color = { fg = "#ff9e64" },
          },
          "encoding",
          "fileformat",
          "filetype",
        },
      },
    })
<
==============================================================================
4. USAGE                                                   *ajapopaja-usage*

TRANSFORMING CODE ~
1.  Select a block of code in Visual Line Mode (V-line) or stay in Normal
    Mode for the whole buffer.
2.  Press `<leader>at` (free text transformation prompt) or `<leader>as` (select
    standard prompt).
3.  In the free form prompt window, enter your transformation request (e.g.,
    "Refactor this into a class"). Press <CR> in normal mode to prompt.
4.  The LLM processes the request. Once finished, use `<leader>ap` to apply
    the result to the initial selection. Alternatively, use `<leader>ai` to
    insert at the current cursor position or to replace the current selection.

Note: When `<leader>ap` is used the original selection is only replaced if the
selection content wasn't change in the mean time. In such a case, use
`<leader>ai` to replace the current visual selection or insert at the cursor
position normal mode.

ITERATE ON A HISTORY ITEM ~
After a transformation, the resulting code can be inspected in the history
window (`<leader>aw`). Use [i/I] to iterate on the result and prompt the LLM
to refine the output. When prompting a refinement, the result is appended to
the history as a new transformation result pointing to the original selection
for replacement.

REVIEWING CODE ~
1.  Select code and press `<leader>ar`.
2.  The review will be generated and stored in the history.
3.  Access the review via the history window (`<leader>aw`).

==============================================================================
5. COMMANDS                                               *ajapopaja-commands*

*:AjapopajaHistory*
    Opens the interactive floating history window. Allows for browsing and 
    applying past transformations or reviews. See |ajapopaja-history-window|.

*:AjapopajaSelectModel*
    Opens a selection UI populated with models currently available on your 
    Ollama instance.

*:AjapopajaRefreshModels*
    Manually triggers a refresh of the available LLM models list from the 
    Ollama API.

*:AjapopajaTransform*
    [Range] Prompts for a multi-line natural language instruction to 
    transform the current selection or buffer. Supports line ranges.

*:AjapopajaSelectPrompt*
    [Range] Opens the prompt library picker for the current filetype. 
    Applies the chosen transformation to the selected range.

*:AjapopajaReview*
    [Range] Sends the selected code to the LLM for analysis. The resulting 
    Markdown review is stored in the review history.

*:AjapopajaApplyLatest*
    Replaces the original selection of the most recent transformation 
    interaction with the LLM's generated output.

*:AjapopajaInsertLatest*
    Inserts the latest transformation result at the current cursor 
    position. Does not replace existing text.

*:AjapopajaEditPrompts*
    Opens the directory containing your Markdown prompt library in the 
    default file explorer. Useful for rapid customization.

==============================================================================
6. KEYBINDINGS                                            *ajapopaja-mappings*

The following default mappings are registered if `opts.default_keymaps` is 
not set to `false` in |ajapopaja-config|.

| Key        | Mode | Command                | Description                    |
|------------|------|------------------------|--------------------------------|
| <leader>as | n, v | AjapopajaSelectPrompt  | Select standard prompt         |
| <leader>ai | n, v | AjapopajaTransform     | Prompt for transformation      |
| <leader>ar | n, v | AjapopajaReview        | Trigger code review            |
| <leader>at | n    | AjapopajaApplyLatest   | Apply latest to buffer         |
| <leader>ap | n, v | AjapopajaInsertLatest  | Insert/Replace latest          |
| <leader>ah | n    | AjapopajaHistory       | Open History Window            |
| <leader>am | n    | AjapopajaSelectModel   | Select LLM Model               |
| <leader>aM | n    | AjapopajaRefreshModels | Refresh model list             |
| <leader>ae | n    | AjapopajaEditPrompts   | Open prompt library dir        |

==============================================================================
7. HISTORY MANAGEMENT                                    *ajapopaja-history*

Ajapopaja maintains a persistent history of interactions. 

- History items are categorized by "call type" (transform, review).
- The history window allows you to preview responses before applying them.
- Persistent storage is managed by the Python backend in `~/.ajapopaja/`.

==============================================================================
8. THE HISTORY WINDOW                                     *ajapopaja-history-window*

The History Window is a sophisticated, interactive interface designed for 
browsing, managing, and applying past LLM interactions. It can be triggered 
via |:AjapopajaHistory| or the default mapping `<leader>aw`.

The window is rendered as a centered floating buffer using Markdown for 
clear formatting and syntax highlighting of code responses.

INTERACTION MODES ~

The window operates in two primary modes, which can be toggled:
- **Transform**: For code refactors and modifications.
- **Review**: For textual feedback and code analysis.

NAVIGATION & CONTROLS ~

The following buffer-local mappings are active within the history window:

| Key     | Action                                                            |
|---------|-------------------------------------------------------------------|
| l       | Navigate to the next history item                                 |
| h       | Navigate to the previous history item                             |
| t       | Switch view to "Transform" history                                |
| r       | Switch view to "Review" history                                   |
| i       | Iterate: Trigger a custom refinement on the current item          |
| I       | Standard Refine: Use a library prompt to refine the current item  |
| x       | Delete the current history entry                                  |
| C       | Clear all history for the current view (Transform or Review)      |
| <CR>    | Apply: (Transform only) Replace buffer text with this result      |
| q       | Close the history window                                          |

WORKFLOW: ITERATION ~

One of the most powerful features of the History Window is the ability to 
"refine" or "iterate" on a previous output. By pressing `i`, you can 
provide additional instructions to the LLM based on its previous response. 
The plugin automatically handles the context transfer, ensuring the model 
understands the current state of the code transformation.

WINDOW DYNAMICS ~

- **Auto-Sync**: The window's title and content automatically update if 
  history changes in the background.
- **Visuals**: Uses `rounded` borders and calculates dimensions (80% of 
  editor width/height) dynamically to fit your workspace.
- **Cleanup**: When closing the window (`q`), your cursor is automatically 
  returned to the original window (`prev_win`).

==============================================================================
8. PROMPT LIBRARY                                         *ajapopaja-prompts*

Ajapopaja uses a filetype-aware prompt library. This allows you to define 
specialized instructions for different programming languages.

PROMPT STORAGE ~
Prompts are stored as Markdown files in the `/prompts` directory of the 
plugin root:
    - `default.md`    (Fallback for any filetype)
    - `python.md`     (Python specific prompts)
    - `javascript.md` (JavaScript specific prompts)
    - `lua.md`        (Lua specific prompts)

MARKDOWN FORMAT ~
Each file contains one or more prompts. A prompt starts with a top-level 
heading (`#`) which acts as the title displayed in the selection UI. All 
text following the heading until the next heading is treated as the prompt 
content.

Example `python.md`:
>markdown
    # Add Docstring
    Write a Google-style docstring for this Python function. 
    Include Args, Returns, and Raises sections.

    # Refactor to Type Hints
    Add PEP 484 type hints to this code. Ensure all function 
    signatures and variables are explicitly typed.
<
BEHAVIOR AND FALLBACKS ~
When you trigger the prompt selection UI (`<leader>as`):
1.  The plugin detects the current buffer's 'filetype'.
2.  It looks for a corresponding `.md` file in the `/prompts` directory.
3.  If the specific file does not exist, it falls back to `default.md`.
4.  Prompts are cached in memory after the first read to ensure the UI 
    remains snappy.

CUSTOMIZING PROMPTS ~
To add your own prompts, simply create a Markdown file named after the 
language's filetype in the plugin's `prompts/` directory. If you are using 
a plugin manager like `lazy.nvim`, you can find this path via:
>lua
    print(vim.api.nvim_get_runtime_file("prompts/default.md", false)[1])
<

==============================================================================
vim:tw=78:ts=8:ft=help:norl:
